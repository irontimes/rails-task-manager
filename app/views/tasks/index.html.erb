<h1> HELLO DRINK COFFEE</h1>

<ul>
<% @tasks.each do |task|  %>
<li> <p><%= task.completed ? '[x]' : '[ ]' %></p> <%= link_to task.title, task_path(task), class: "btn btn-primary" %></li>

<%end  %>

</ul>
